class Console {
  make x;
  make y;

  $ Constructors

  make Console(x, y) {}

  make log() {
    Impala.stdout.write(x);
    Impala.stdout.write(y);
    Impala.stdout.flush();
  }
};

log(Console("hi", "there").log());